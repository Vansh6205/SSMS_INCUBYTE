class Sweetshopmanagementsystem {
    #availableSweet = [];

    // Method to add a book to the library
    addSweet(book) {
        this.#availableSweet.push(sweet);
        console.log(`Sweet${sweet.ISBN} added successfully!`);
    }
}